# Site-wide page transition (zitafernandez.com style)

**Status:** current Framer draft code files are source of truth · 2026-06-17 · codeFile `gmalnRr` (`PageTransition.tsx`) insertUrl `https://framer.com/m/PageTransition-br4HFc.js@edhIffK73jUlQYx83Xk0` wraps the v7.12 runtime with the Home Header Bottom recovery · `/index` codeFile `rgAZFOv` (`IndexPage.tsx`) insertUrl `https://framer.com/m/IndexPage-msQHCf.js@TTjkvY4y86uWzgMbhCgf` · public publish/route QA still needed for the normal `khaki-ship-257706.framer.app` bundle

**Motion ease canon — June 17 consolidation:** coded Framer components now use two main curves. Snappy `cubic-bezier(0.16, 1, 0.3, 1)` owns decelerating ease-out motion: index nav/media fades, type masks, hover transforms, thumbnail scale, and prior `cubic-bezier(0.22, 1, 0.36, 1)` decel uses. Smooth `cubic-bezier(0.12, 0.23, 0.5, 1)` owns symmetric in/out motion: line/rule draws, small UI state transitions, and prior `cubic-bezier(0.33, 0, 0.67, 1)` or bare `ease` transitions.

**v7.12 addition — `/index` single-owner heading and smoother content reveal:** the earlier explicit custom replay for the top `Index` heading was removed because it could animate the heading twice. The transition layer now uses `data-pt-index-heading-hold` only as a same-document route safety pin, clears it with `releaseIndexHeadingHold()`, and skips the top `Index` heading in generic appear replay on `/index`; Framer's native heading appear animation owns the visible title reveal. `IndexPage.tsx` now handles the rest of the page with row-ordered motion: taxonomy nav fades top row to bottom row via `INDEX_NAV_FADE_PRESET`, list text uses `INDEX_CONTENT_REVEAL_PRESET` so offscreen rows have already revealed before the user scrolls down, and grid thumbnails fade through `GridMediaFrame` with `INDEX_MEDIA_FADE_PRESET` (620ms, 140ms base delay, 58ms item stagger, Snappy `cubic-bezier(0.16, 1, 0.3, 1)`).

**v7.11 addition — shared Info-style type reveal timing:** long native Framer translate reveals now normalize to one site-wide type profile: 900ms duration, 90ms base delay, 90ms stagger, Snappy `cubic-bezier(0.16, 1, 0.3, 1)`, and a `115%` hidden start. This replaces the older route-specific Home and `/index` timing tweaks so `Micah Hoang`, the Home descriptor, and other long type masks enter with the same rhythm as the `/info` page. Direct arrivals to Home replay those type reveals when no active View Transition is present, which covers plain case-study returns. As of v7.12, the top `/index` heading is excluded from this generic replay and left to Framer's native heading animation.

The current Framer draft code file contains the integrated v7.12 runtime source from `PageTransition.tsx`. If pushing a generated runtime later, preserve the same timing constants, the `/index` heading skip, and the `.idx-mask-reveal-text` normalizer.

**v7.11 `/index` mask normalizer:** the custom `IndexPage.tsx` WAAPI masks (`.idx-mask-reveal-text`) are normalized at runtime to the same 900ms / 90ms rhythm, `115px` start distance, and Snappy easing. This keeps list years, list titles, and grid titles aligned with the shared type rhythm.

**v7.10 addition — `/index` heading hard hold (superseded by v7.12):** the `/index` native heading originally paired `data-pt-index-heading-hold` with a direct WAAPI replay for the top `Index` layer. v7.12 keeps the hold/release safety behavior but removes the direct replay so the title cannot animate twice.

**v7.9 addition — no page transition on case-study legs:** case-study routes are now an explicit bypass surface. Any same-document click whose source or destination is `/case-studies` / `/case-studies/*` returns before `preventDefault()` and before `document.startViewTransition()`, so Framer/browser navigation runs plainly. Cross-document case-study documents install `@view-transition { navigation: none; }` instead of `auto`, and the `pageswap` fallback calls `skipTransition()` when the old or destination document is a case study. This deliberately trades away the heavy sheet transition only for Home ↔ case-study and case-study exits, after repeated live QA showed case-study hydration/media load could still expose white flashes or footer fragments. Non-case routes keep the normal page-transition system.

Published QA on 2026-06-12 used the in-app browser against `https://khaki-ship-257706.framer.app`: home served `script_main.lUnpadwT.mjs`, Home carried `@view-transition { navigation: auto; }` and no eager `/case-studies/*` speculation rule; clicking the actual AirPods selected-work card landed on `/case-studies/airpods` with `data-pt-case-study-transition="off"` and `@view-transition { navigation: none; }`; clicking the actual Work nav returned to `/` with `navigation: auto`. Rapid frame samples reported `:active-view-transition === false` throughout both legs and `footerVisible === false` on every return sample. Screenshots live under `output/playwright/case-study-transition-bypass-2026-06-12/`.

**v7.8 addition — `/index` heading timing (superseded by v7.11):** `/index` originally got a route-specific replay profile for long native Framer translate reveals. v7.11 replaces that special delay with the shared Info-style type profile above.

**v7.7 correction — prefetch, not prerender:** published v7.6 was still observed on `https://khaki-ship-257706.framer.app` with speculation rules that prerender `/case-studies/*` from Home and prerender `/`, `/index`, `/play`, `/info`, plus sibling case studies from a case-study page. That matches the remaining symptom: a prerender can activate before Framer hydration/IntersectionObserver-driven layout has fully settled, so the browser may reveal half-built page state such as footer rows or card content in the wrong visual moment. v7.7 removes every `prerender` rule and keeps only path-aware document `prefetch`: eager case-study prefetches from Home, immediate core-route prefetches from case-study pages, and eager sibling-case-study prefetches. The runtime now uses a `data-pt-prefetch` marker and removes stale `data-pt-prerender` scripts if found.

**v7.6 addition — load-time prerender:** v7.5's `moderate` (hover-triggered) prerendering still lost to fast first/second clicks (user-confirmed on published v7.5). The speculation rules are now path-aware and fire at hydration instead of hover for the flash-prone native legs: on home, `{where: href_matches "/case-studies/*", eagerness: "eager"}` prerenders every Selected Work card destination as soon as the effect runs (Chrome caps eager+immediate prerenders at 10); on case-study pages, `{urls: ["/", "/index", "/play", "/info"], eagerness: "immediate"}` (every exit there is native because of the lightbox guard) plus the eager sibling-case-study rule (Other Projects cards). The universal moderate prefetch+prerender hover rules remain for everything else. Speculation-rules scripts are immutable once inserted, so the effect removes + re-adds the script when the desired rule set changes (SPA navs). Marker id `data-pt-prerender`.

**v7.5 addition — hover-prerender kills the cross-document white gap:** after v7.4 published, the user still saw a brief white flash before the swipe on case-study navigations specifically (both directions), and nowhere else. Live probing mapped the navigation modes: Framer's router accepts untrusted re-fired clicks on its own Link components (nav links → SPA preserved), but its global anchor listener is **trusted-only**, so the custom Selected Work card anchors (`.selected-work-card`, plain DOM `<a>`) fall through to NATIVE cross-document navigation under v7.4's click-swallow — and case-study exits were always native (lightbox guard). The white flash lives in the inter-document fetch/parse gap (Chrome's paint-holding gives up on slow documents). Fix: the speculation-rules script now declares `prerender` (plus `prefetch`) with `moderate` eagerness for all internal URLs — hover prerenders the destination, activation is instant, and **the cross-doc view transition still runs on prerender activation (live-verified: 6 `::view-transition` pseudo animations, full ~1.1s choreography, URL swap within 84ms of click, end-to-end through the click-swallow machinery)**. Bonus: prerendered case studies arrive fully rendered, hero video first frames included. Script marker id changed `data-pt-prefetch` → `data-pt-prerender`.

**v7.4 addition — case-study entry smoothness (user-reported "white flash + laggy animate-on"):** three changes, all driven by per-frame screencast analysis of home → /case-studies/airpods on the published site. (1) **Deterministic old-capture restored** — v7.2 had reverted v7.1's click-swallow/re-fire and the pre-transition flash of the blank destination returned on warm caches (the router can swap the DOM inside the click task, before the browser snapshots the old page; the cost of serializing is ~one frame). The original click is `preventDefault`ed + `stopImmediatePropagation`ed and re-fired via `anchor.click()` inside the `startViewTransition` update callback; `sdSynth` passes the synthetic click through; 1.2s href-unchanged fallback assigns directly. (2) **`RELEASE_AT` 0.55 → 0.40** — at 0.55 the incoming sheet slid for ~450ms as a completely blank cream surface (every appear element preplay-held at opacity 0), which is what actually read as the "white flash" on case studies. (3) **Image-decode warming during the hold** — releasing dozens of opacity-0 images at once forced a raster/decode burst that stalled the compositor ~70ms exactly at the mid-slide release (measured 71ms frame gap); the hold now calls `img.decode()` on all appear-held subtrees + `video[poster]` posters at arm and again at +180ms, and the appear-defs JSON is parsed once per page (`sdDefs` cache) instead of on every replay pass. Remaining known gap on case-study entries: hero videos without posters stay a grey box until their first frame arrives over the network (cargo.site latency) — that is a media problem (posters / de-cargo per the 2026-06-12 media-compression audit), not a transition problem.

**v7.3 addition — home re-entry timing (superseded by v7.11):** clicking back to Home/Work from another route originally used an earlier appear release and a capped replay profile for the long masked home hero appears. v7.11 keeps the earlier Home release but moves the long type timing itself into the shared Info-style profile.

**v6.7 addition — centered boot identity:** the home first-boot curtain now includes a centered Cream `Micah Hoang ©2026` mark on Forest Green. It uses the same `/Heading 3` GT Standard L Regular treatment as the home hero descriptor (`Brand designer with a systems mind`): 30px desktop, 24px tablet, 19px mobile, 120% line height, and -0.01em tracking. It fades out during the final progress-bar completion before the existing upward homepage swipe begins. The global transition CSS also suppresses Framer's public `#__framer-editorbar-container` / `#__framer-editorbar-label` overlay if Framer injects it on published routes.

**v6.6 addition — transition-complete load-ins everywhere:** same-document route changes now arm the global appear hold inside the `startViewTransition()` update callback, immediately after the incoming route renders and before the browser captures the new state. That keeps Framer/native load-in animations at frame zero while the page sheet is moving, then releases/replays them only after `viewTransition.finished`. Custom components should follow the same contract: if they see `:active-view-transition`, wait for `pt:reveal` or the active-transition fallback before starting their own intros.

**v6.5 addition — home-only first boot:** the Forest Green curtain + Cream progress bar now plays only on the home route (`/`) in Auto mode: direct home entries and home reloads still get the full opening loader, while `/work`, `/play`, `/index`, case studies, and other route refreshes skip the site-level boot curtain and let their own page load-in animations run normally. The hidden `/play` gallery hold still applies pre-paint and now has an explicit direct-refresh release path so the archive can reveal through its local load-in.

**v6.4 additions — appear restart that actually fires + /play handling:** live probing showed Framer's `startOptimizedAppearAnimation` silently no-ops when re-invoked after the hydration handoff — the v6.3 replay ran but animated nothing. v6.4 builds the WAAPI animations **directly** from the published appear definitions (`__framer__appearAnimationsContent` + breakpoint hash) with `el.animate()` — verified live on /index (6 effects restarted, correct ease/duration). Each replay dispatches a **`pt:reveal` CustomEvent** on window: the contract for custom code components to restart their own intros. `Play.tsx` now listens and replays a staggered opacity fade across the archive cards (opacity only — transforms belong to the drift/parallax engine). PageTransition also carries a **/play force-blank hold** (`data-playground-force-blank` attr + CSS, applied pre-paint by the installer script and on transitions into /play, released when the transition settles) so the play gallery never peeks mid-transition.

**v6.3 additions — appear-effect RESTART (cause and effect):** load-in animations (text fades, header slide-ups, line draws) now start only when the cover ends, even if they already ran while preloaded. New `replayAppearEffects()` re-invokes Framer's own appear runtime (`window.animator.animateAppearEffects` + the `window.__framer__appearAnimationsContent` definitions — the exact call Framer's inline starter makes) to restart every appear effect from its initial state. The **boot loader** calls it at swipe start (`window.__ptReplayAppear`), while the curtain still fully covers, so the reset is invisible and effects animate in as the page is revealed. **Page transitions** keep the frame-zero hold during the slide and replay anything the hold missed at release (held elements are skipped to avoid double-starts). Caveat: SPA-router navigations rely on the hold + the new page's natural mount animations (the appear definitions JSON belongs to the initially loaded document).

**v6.2 additions:** (1) **Zita-style first-boot gating** — the loader plays on qualifying fresh entries/reloads and skips internal-link arrivals (page transition owns those), back/forward, and prerender passes. As of v6.5, Auto mode also requires the current route to be home (`/`). Detection: route + navigation timing entry type (`reload` → play only on home; `navigate` + empty/external referrer → play only on home) — no sessionStorage at all (the old `__ptBootSeen:v2` key is cleaned up for returning visitors). `Boot mode` options are **Auto** (home entries/reloads, default — legacy value "once") and **Always** (every home document load). (2) **Nav name dedup guard** — duplicate `view-transition-name`s on one page silently disable the whole transition per spec; the component now keeps the name on the first *rendered* nav and sets `view-transition-name: none` on any others, applied at mount, before the same-document old-state capture, and before the new-state capture.

**v6.1 additions:** (1) smoother page easing — `cubic-bezier(0.6, 0, 0.18, 1)` (softer entry, silkier landing) replaces `(0.76, 0, 0.24, 1)`. (2) **Appear-effect hold** (`Hold appear` prop, on by default): entrance animations on the incoming page are frozen at frame zero while the sheet is moving and released the moment the transition finishes — the transition visibly *causes* the load-ins. Hooks: `pagereveal.viewTransition.finished` (cross-document, plus an `:active-view-transition` polling fallback if the module evaluates late) and, as of v6.6, the same-document hold is armed inside the `startViewTransition()` update callback before the new-state capture. Loops, ambient animations (running > 1.6s), and CSS transitions are left untouched; held animations inside the nav are fast-forwarded instead of replayed (the nav has its own VT choreography). 4s hard-safety release.

## What it is

**Dual-path View Transitions, with a case-study bypass.** Framer's published runtime navigates internal links two ways: its SPA router (`history.pushState` — e.g. nav links on pages without the lightbox click-guard) and real cross-document loads (e.g. on case-study pages where the guard stops the router's listener). v6+ covers both for normal routes:

1. **Cross-document path:** `@view-transition { navigation: auto }` CSS — the browser snapshots the outgoing page and animates between documents.
2. **Same-document path (new in v6):** a module-scope capture listener wraps qualifying internal link clicks in `document.startViewTransition()` *without* preventing default — Framer's router still performs the pushState navigation inside the transition's update phase. The update callback polls (timers, not rAF — rendering is paused) for URL change + DOM mutation/title change, then resolves; if the click turns out to be a full document load, the same-document transition is abandoned at unload and path 1 takes over; if the router never produces new content within 2.5s, `skipTransition()` fires so a stale snapshot is never animated.

Both paths share the same `::view-transition-*` pseudo-elements, so one set of keyframes animates both: the outgoing page dims to 35% and drifts up 10vh while the **actual incoming page slides up over it as a sheet** (700ms, `cubic-bezier(0.6, 0, 0.18, 1)` as of v6.1); media still loading appears on top of the moving sheet. The nav is its own transition group (`view-transition-name: __pt-nav`): it swipes up with the old page (~340ms) and swipes back down 400ms after the sheet lands using Snappy `cubic-bezier(0.16, 1, 0.3, 1)`.

As of v7.9, any navigation entering or leaving `/case-studies` bypasses both paths and uses plain Framer/browser navigation. This keeps case-study hydration, hero media, lightbox guards, and footer/layout settlement out of the page-transition critical path.

The **first-boot loader** (carried from v5.2, re-implemented during the v6 merge): the script injects a fixed Forest Green curtain (`#233324`, Framer color style `/Forest Green`), centered `Micah Hoang ©2026` identity text, and an 8px Cream top progress bar before hydration. The identity text uses the home hero descriptor's `/Heading 3` treatment: GT Standard L Regular, 400 weight, 120% line height, -0.01em tracking, and responsive sizes of 30px desktop / 24px tablet / 19px mobile. The bar animates with Zita Fernandez's loader curve (`cubic-bezier(0.65, 0.01, 0.05, 0.99)`), the identity text fades out during the final bar completion, then the curtain swipes up over 1.2s to reveal the loaded page. On by default; respects `prefers-reduced-motion`. As of v6.5 it plays only on home direct entries and home reloads in Auto mode; non-home refreshes skip the boot curtain.

The component renders an SSR'd inline `<script>` that installs the CSS (`@view-transition { navigation: auto }` + keyframes + first-boot loader rules) into `<head>` during HTML parse (v5.1: script-install instead of a React `<style>`, which caused recoverable hydration warnings), plus a Speculation Rules **hover prefetch** so the swap starts promptly after a click, plus one-time cleanup of legacy (v1–v4 curtain) state for returning visitors.

For internal navigation there is **no click interception and no curtain handoff** — v1–v4's navigation curtain machinery was retired because it had an irreducible prerender-activation race (the new page could paint un-curtained for a frame → "bright flash" / "content peek"). The browser composites both documents itself, so no blank frame is possible. v5.2 uses a separate first-boot-only seed curtain and a `sessionStorage` replay guard; that guard is not part of internal route handoff.

## Props (current, v7.12)

`Enabled` · `Slide` (700ms) · `Nav swipe` (400ms) · `Old drift` (10vh) · `Old dim` (0.35) · `Nav selector` (`nav[data-framer-name="Navigation"], nav`) · `Exclude` (`[data-no-transition]`, same-document path only) · `Prefetch` (Hover/Off) · `Hold appear` (Until done/Off) · `Case studies` (Skip/Animate, default Skip) · `First boot` (Show/Off) · `Boot mode` (Auto/Always) · `Boot color` (`#233324`) · `Bar color` (`#F7F5F0`) · `Bar height` (8px) · `Boot min` (1200ms) · `Max wait` (4500ms) · `Bar ease` (3000ms) · `Bar hold` (0.86) · `Boot swipe` (1200ms) · `Swipe delay` (300ms)

## Verified working (2026-06-11, user-confirmed "almost perfect" before later v7.x local polish)

The behaviors below were confirmed working on the published site before the v7.12 `/index` timing polish. The v7.12 code files are pushed to Framer draft and typecheck cleanly, but the public `khaki-ship-257706.framer.app` bundle still needs a Framer Publish before live QA can confirm the final timing there:

- **Page-to-page swipe** on BOTH navigation paths: SPA router clicks (home → info etc., wrapped `startViewTransition`) and full document loads (case-study pages, `@view-transition`). The incoming page slides up as a sheet over the dimming, drifting old page — the Zita layering, with media loading in on top of the sheet.
- **Nav choreography**: swipes up with the outgoing page, swipes back down after the sheet lands; no re-render flash (its VT group covers it on both paths).
- **No white/black flash, no content peek** — eliminated structurally by the View-Transitions architecture (browser composites both pages; nothing paints in between).
- **Appear-effect causality (v6.1)**: entrance animations on the incoming page hold at frame zero during the slide and play the moment the transition finishes — the transition visibly causes the load-ins. Verified in the harness: held mid-slide, released within ~100ms of `finished`, completed after.
- **First-boot loader** on home direct entries and home reloads only (Forest Green curtain + centered `Micah Hoang ©2026` identity in the home descriptor `/Heading 3` style + Cream progress bar, Zita's loader curve; v6.5 Auto gating — non-home refreshes, internal-link arrivals, and back/forward skip it).
- **Safety paths**: stale-content skip guard (router stalls → instant swap, never a stale-snapshot slide), hash/external/modifier-click/`[data-no-transition]` exclusions, reduced-motion instant, Firefox instant, 4s hard-safety on the appear-hold.
- **Case-study bypass (v7.9)**: Home → AirPods and AirPods → Home are plain navigation. In-app QA sampled both legs with no active View Transition and no footer visibility during return.

Local regression suites (all green at snapshot time): /tmp/ptqa — 14 curtain-era tests, 13 v4 choreography tests, 7 same-document wrapper tests, 5 appear-hold tests. Live boot-identity QA: `output/playwright/boot-hero-style-qa/qa-hero-style-final-result.json` (46/46 checks across desktop, tablet, mobile, plus non-home boot skip).

## Placement

One instance per page, **first child of the Desktop root** (1×1, opacity 0). For cross-document transitions both the outgoing AND incoming page must carry an instance; navigations involving an instance-less page are plain instant loads (graceful).

**Placed (updated 2026-06-15):** home, /info, /index, /play, /case-studies, /case-studies/:slug template, /404, and all 17 current bespoke CMS case-study pages. /contact was an unpublished draft and has been deleted from the project.

## Degradation matrix

- Firefox (no cross-document VT): instant navigation, no artifacts.
- `prefers-reduced-motion: reduce`: all transition animations disabled → instant swap.
- Back/forward: plays the same upward slide.
- Pages without the component: instant navigation.

## Gotchas

- **Stale CDN cache can hide the CSS**: the site serves `cache-control: max-age=0, must-revalidate` with `Vary: Accept-Encoding, Accept`, so different clients can hit older cached variants for a while after publish. A 2026-06-10 audit reported the CSS "missing" from yomo/weaponized-innocence/seek-truth — fresh origin fetches showed it present on all of them. Verify with a cache-busting query before concluding a page is missing coverage.
- **First-boot testing**: in Auto mode, reload the home page (`/`) to see the loader. Reloading `/work`, `/play`, `/index`, case studies, or any non-home route should skip the Forest Green curtain and only run that page's own load-in behavior.
- **Framer editorbar**: the global CSS hides Framer's injected `Edit Content` editorbar on published routes; this is not the removed custom editorbar guard component.
- Duplicate `view-transition-name` on one page silently **skips the whole transition** — the nav selector must match exactly one *rendered* nav per page (hidden breakpoint variants don't count; `display:none` elements aren't captured).
- Like all MCP code edits: manual **Publish** required.
- QA harnesses: /tmp/ptqa (local two-page suite), /tmp/lbqa (live-site puppeteer probes).

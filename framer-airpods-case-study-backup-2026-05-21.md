# Framer AirPods Case Study Backup - 2026-05-21

Read-only backup before responsive media edits on Framer page `LB7pYBD3k`
(`/case-studies/airpods`), Desktop root `I7h6iPgRE`.

**Current note - May 26, 2026:** `/case-studies/airpods` is still the bespoke AirPods page in the current Framer web-page inventory. Active custom helpers on the page are `RelatedProjectHoverZoom.tsx` (`GTEGUfN`) and `OtherProjectCardRestored.tsx` (`vlwa5Cz`). `CaseStudyRevealTuner.tsx` was removed from Framer during the May 26 cleanup because it was not mounted in the current AirPods page XML; do not describe it as part of the active page.

## Primary Targets

- Hero media wrapper `QbQy0SVpT`: `height="640px"`, `aspectRatio="1.875"`.
- Hero video `vjYhgcGZI`: `width="100%"`, `height="100%"`, `objectFit="cover"`.
- Media section `p4eV2Tc_G`: `gap="20px"`, `padding="0px 20px 160px 20px"`.

## Gallery Rows Before Edit

- `wtds3Pbdk`: `height="335px"`, `stackDirection="horizontal"`, two wrappers `dVCeKDkal` / `PR5iPzUzO` at `width="0.5fr"`, videos `i991AHbO7` / `V4f4p3ycD` at `height="328px"`, `objectFit="cover"`.
- `JCEa_wjH_`: `height="640px"`, `aspectRatio="1.813"`, image `C90qtVP81` at `height="100%"`.
- `iUmhJ9HaR`: `height="fit-content"`, direct video `GsyZsXKqu` at `height="668px"`, `objectFit="cover"`.
- `ySsrd6ap1`: two wrappers `Qj4h5mmdd` / `PLvrIKa7V` at `width="0.5fr"`, videos `gxxFUvipy` / `m9PgckPh7` at `height="328px"`, `objectFit="cover"`.
- `dXMDC8ipE`: wrapper `VwLEcOgeV` at `height="640px"`, `aspectRatio="1.813"`, image `MhinnVLfa` at `height="100%"`.
- `R4GHEeJKb`: two wrappers `KKFwq_opn` / `j6I0GIOEO` at `width="0.5fr"`, videos `hLXEWMapO` / `dJa72OIDf` at `height="328px"`, `objectFit="cover"`.
- `TGYmF7mJ4`: direct image `wI90ldKoE` at `width="1160px"`, `height="651px"`.
- `kB8sMmTZ3`: wrappers `Fb4wPU4AB` at `height="1fr"` and `mjHyEViPC` at `height="fit-content"`; images `c796EFfUt` at `height="100%"`, `m3GOvdbZl` at `height="610px"`.
- `w8Y5HNNM3`: wrapper `jRHYGykhZ` at `height="668.5px"`, video `NTtDEHiSb` at `height="1fr"`, `objectFit="cover"`.

## Related Project Layout Before Edit

- `hCtH8P9BS`: `stackDirection="horizontal"`, no `stackWrap`.
- `otWiMrxfA`, `kepaZiNbC`, `ik9cy7cBI`: each `width="1fr"`, no `minWidth`.

## Metadata/CMS Observation

- Category row `NhVz4lX1Z` is static text: `Visual Identity / 2D Motion / 3D Motion`.
- Metadata container `BpswbUqS4` is static text for about, industry, credits, client, and year.
- Current project strategy notes say bespoke pages should keep custom storytelling, while the header/metadata block can be CMS-bound later.
